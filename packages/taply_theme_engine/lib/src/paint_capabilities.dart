/// Defines the capability level of a painter.
enum PaintCapabilities {
  /// Basic painting (simple shapes, solid fills).
  basic,

  /// Advanced painting (gradients, shadows, transforms).
  advanced,

  /// Full painting (complex effects, masks, blend modes).
  full,
}

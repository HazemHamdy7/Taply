import '../models/theme_scene.dart';

/// Parses scene definitions from JSON.
class SceneParser {
  /// Parses a single [ThemeScene] from a JSON map.
  ThemeScene parseScene(Map<String, dynamic> json) {
    throw UnimplementedError('SceneParser.parseScene');
  }

  /// Parses a list of [ThemeScene] objects from a JSON list.
  List<ThemeScene> parseScenes(List<dynamic> jsonList) {
    throw UnimplementedError('SceneParser.parseScenes');
  }
}

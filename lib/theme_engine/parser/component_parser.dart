import '../models/theme_components.dart';

/// Parses component schema definitions from JSON.
class ComponentParser {
  /// Parses a [ComponentSchema] from a JSON map.
  ComponentSchema parseSchema(Map<String, dynamic> json) {
    throw UnimplementedError('ComponentParser.parseSchema');
  }

  /// Parses a list of [ComponentSchema] objects from a JSON list.
  List<ComponentSchema> parseSchemas(List<dynamic> jsonList) {
    throw UnimplementedError('ComponentParser.parseSchemas');
  }
}

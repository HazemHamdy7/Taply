import '../models/theme_document.dart';

/// Loads themes from network URLs.
class NetworkLoader {
  /// Loads a theme from a URL.
  Future<ThemeDocument> load(String url) {
    throw UnimplementedError('NetworkLoader.load');
  }
}
